
import React from 'react';

// This component is a placeholder for a more detailed audit log inspection UI.
// It could be used to display the full input/output of an agent when a log entry is clicked.
const AuditInspector: React.FC = () => {
    return (
        <div className="p-4">
            <h2 className="text-lg font-bold">Audit Inspector</h2>
            <p className="text-slate-400 mt-2">Select a log entry to see details.</p>
        </div>
    );
};

export default AuditInspector;
