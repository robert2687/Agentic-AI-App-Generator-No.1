# Android Keystore Configuration Template
# Copy this file to keystore.properties and fill in your actual values
# NEVER commit keystore.properties to version control!

storePassword=YOUR_KEYSTORE_PASSWORD_HERE
keyPassword=YOUR_KEY_PASSWORD_HERE
keyAlias=my-key-alias
storeFile=../my-release-key.keystore

# To generate a keystore file, run:
# keytool -genkey -v -keystore my-release-key.keystore -alias my-key-alias -keyalg RSA -keysize 2048 -validity 10000

# After creating keystore.properties, update android/app/build.gradle
# See ANDROID_BUILD_GUIDE.md for detailed instructions
